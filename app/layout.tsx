import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/navigation/navbar";
import Footer from "@/components/navigation/footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Morsan HR Consulting",
  description:
    "Morsan HR Consulting is a leading provider of comprehensive human resource solutions designed to empower businesses and enhance workforce performance. We specialize in delivering innovative HR services tailored to meet the unique needs of organizations across various industries, including healthcare, technology, manufacturing, retail, education, and finance. Our expertise spans talent acquisition, employee relations, HR compliance, performance management, and workforce planning, making us a trusted partner for companies seeking to optimize their human resource operations.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Navbar />
        {children}
        <Footer />
      </body>
    </html>
  );
}
